apiVersion: v1
kind: Pod
metadata:
  name: superdeploy-app
  labels:
    app: superdeploy-app
spec:
  containers:
    - name: superdeploy-app
      image: git.servidordomal.lol/robogg133/superdeploy:latest
      ports:
        - containerPort: 80
