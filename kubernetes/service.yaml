apiVersion: v1
kind: Service
metadata:
  name: superdeploy-service
  namespace: superdeploy-namespace
spec:
  selector:
    app: superdeploy-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
