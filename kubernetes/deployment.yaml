apiVersion: apps/v1
kind: Deployment
metadata:
  name: superdeploy-app
  namespace: superdeploy-namespace
  labels:
    app: superdeploy-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: superdeploy-app
  template:
    metadata:
      labels:
        app: superdeploy-app
    spec:
      serviceAccountName: superdeploy-app
      containers:
        - name: superdeploy-app
          image: git.servidordomal.lol/robogg133/superdeploy:latest
          envFrom:
            - secretRef:
                name: superdeploy-secrets
          env:
            - name: DEBUG_ENABLED
              value: "true"
          ports:
            - name: http
              containerPort: 8080
            - name: pprof
              containerPort: 6060
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
